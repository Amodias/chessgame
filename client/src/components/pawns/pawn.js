import React from 'react';
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';
import { faChessPawn } from '@fortawesome/free-solid-svg-icons';

const Pawn = () => {
  return <FontAwesomeIcon icon={faChessPawn} style={{ fontSize: '2rem' }} />;
};

export default Pawn;
